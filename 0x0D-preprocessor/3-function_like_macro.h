#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/**
 * The file:3-function_like_macro.h
 * Description:computes the absolute value of a number x
 * @x: the number to work with
 */

#define ABS(x) ((x) < (0) ? -(x) : (x))

#endif
